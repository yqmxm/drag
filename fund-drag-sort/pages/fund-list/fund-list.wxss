.card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px;
  margin: 5px 0;
  border-radius: 5px;
  color: #333;
  font-size: 16px;
}

.movable-view {
  width: 100%;
}

.card-background {
  flex: 1;
  height: 100%;
  border-radius: 5px;
}

.movable-area {
  width: 100vw;
  /* 其他样式 */
}

.fund-card {
  width: 100%;
  box-sizing: border-box;
  /* 其他样式 */
}